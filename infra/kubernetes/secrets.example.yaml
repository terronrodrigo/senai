# Copiar para secrets.yaml e preencher; não versionar secrets.yaml
# kubectl create secret generic saga-senai-secrets --from-literal=DATABASE_URL=... --from-literal=JWT_SECRET=... -n saga-senai --dry-run=client -o yaml > secrets.yaml
apiVersion: v1
kind: Secret
metadata:
  name: saga-senai-secrets
  namespace: saga-senai
type: Opaque
stringData:
  DATABASE_URL: "postgresql://user:pass@rds-endpoint:5432/saga_inovacao"
  REDIS_URL: "redis://redis-host:6379"
  RABBITMQ_URL: "amqp://user:pass@rabbitmq:5672"
  JWT_SECRET: "change-me-min-32-chars"
  OPENAI_API_KEY: ""
